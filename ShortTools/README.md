# ShortTools

This library is the successor to Short_Tools, which had some fundamental issues with the base code, so I have moved it into several separate projects, which will translate into separate packages.

The general functionality should be the same.

This package is the general package, which is required for all of the other packages to function.
Currently, there are 2 other packages:

